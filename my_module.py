def print_list_elements(my_list: list[str]) -> None:
    """In ra từng phần tử trong danh sách.

    Args:
        my_list: Danh sách các chuỗi cần in
    """
    for element in my_list:
        print(element)
